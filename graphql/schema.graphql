type Query {
  tests: [test]!
  findPerson(gender: String!, place: String!): person
}

type test {
  id: Int!,
  name: String!
}

type Mutation {
  addPerson(gender: String!, place: String!): person!
}

type person {
  id: Int!,
  gender: String!,
  place: String!
}
